{
  "name": "vectra",
  "version": "1.1.5",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix vectra-extension && npm install --prefix vectra-web",
    "build": "npm run build --prefix vectra-extension && npm run build --prefix vectra-web",
    "test": "npm test --prefix vectra-extension && npm test --prefix vectra-web"
  }
}
