name: CI

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: |
            vectra-agent-core/package-lock.json
            vectra-extension/package-lock.json
      - run: npm ci --prefix vectra-agent-core
      - run: npm ci --prefix vectra-extension
      - run: npm test --prefix vectra-agent-core
      - run: npm run package --prefix vectra-extension
