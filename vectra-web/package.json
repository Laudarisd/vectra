{
  "name": "vectra-web",
  "private": true,
  "version": "1.1.0",
  "description": "Vectra Web professional AI workspace companion with local/cloud models, pre-parsed PDF/DOCX/PPTX/XLSX attachments, image inputs, GGUF llama.cpp runtime, false-refusal recovery, and downloadable generated documents/code files.",
  "type": "module",
  "scripts": {
    "build:core": "tsc -p core/tsconfig.json",
    "build": "npm run build:core && node scripts/build.mjs",
    "start": "npm run build:core && node server/server.mjs",
    "test": "npm run build:core && node --test --test-isolation=none test/*.test.mjs test/*.test.cjs"
  },
  "dependencies": {
    "deepagents": "1.13.1"
  },
  "devDependencies": {
    "@types/node": "^20.19.43",
    "typescript": "^5.8.0"
  },
  "engines": {
    "node": ">=22.13"
  }
}
